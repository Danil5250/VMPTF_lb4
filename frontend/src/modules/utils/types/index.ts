
export interface IService {
    id: string;
    title: string;
    iconUrl: string;
}

export interface IWorkshop {
    id: string;
    name: string;
    address: string;
    rating: number;
}

export interface IUser {
    id: string;
    name: string;
    email: string;
}